name: Realtime News Notification Bot

on:
  workflow_dispatch:
  schedule:
    - cron: '0 21 * * *'  # 매일 아침 06:00 (KST)
    - cron: '30 4 * * *'  # 매일 점심 13:30 (KST)
    - cron: '30 13 * * *' # 매일 밤 22:30 (KST)

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 저장소 코드 가져오기
        uses: actions/checkout@v4

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 필요 라이브러리 설치
        run: |
          python -m pip install --upgrade pip
          pip install curl_cffi beautifulsoup4

      - name: 뉴스 크롤러 실행 및 텔레그램 발송
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python main.py

      - name: 발송 기록 자동 저장 (중복 발송 완전 차단)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add sent_links.txt
          git commit -m "update sent_links [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:main
